{
  "name": "@adexchangedevops/aeg-limelight-api",
  "version": "0.0.15",
  "description": "Limelight API",
  "main": "./lib/api.js",
  "author": "Justin Durham",
  "repository": {
    "type": "git",
    "url": "git://git@bitbucket.org:AdExchangeGrp/aeg-limelight-api.git"
  },
  "license": "UNLICENSED",
  "scripts": {
    "test": "mocha --recursive test/",
    "test:watch": "npm run test -- -w",
    "test:coverage": "istanbul cover _mocha -- --recursive test/",
    "jshint": "jshint .",
    "jshint:watch": "npm run jshint && onchange \"**/*.js\" \".jshint*\" -- npm run jshint",
    "dev": "parallelshell \"npm run jshint:watch\" \"npm run test:watch\"",
    "build": "mocha --recursive test/unit/ -R xunit-file",
    "postversion": "git push && git push --tags && npm publish ."
  },
  "devDependencies": {
    "should": "^5.2.0",
    "mocha": "^2.2.5",
    "parallelshell": "^1.1.1",
    "onchange": "^1.1.0",
    "xunit-file": "0.0.6",
    "istanbul": "^0.3.17"
  },
  "dependencies": {
    "@adexchangedevops/aeg-logger": "^0.0.12",
    "config": "^1.12.0",
    "querystring": "^0.2.0",
    "request": "^2.53.0",
    "stringformat": "0.0.5",
    "underscore": "^1.8.2"
  }
}
